# =====================================================
# Sondos AI Backend — Environment Variables
# =====================================================
# Copy this file to .env and fill in real values:
#   cp .env.example .env
#
# ⚠️ NEVER commit .env to git!
# =====================================================

# MongoDB Atlas
MONGO_DB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/Sondos-Portal
DB_NAME=Sondos-Portal

# JWT Secret — generate with: openssl rand -hex 64
JWT_SECRET=

# AutoCalls White-Label API Key
AUTOCALLS_API_KEY=

# Admin Seed Password — used only by seedAdmin.js
ADMIN_SEED_PASSWORD=

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Setup Service (Python FastAPI)
SETUP_SERVICE_URL=https://siyadah-whatsapp-saas.onrender.com

# ══════════════════════════════════════════════════════
# Moyasar Payment Gateway (مُيسّر)
# ══════════════════════════════════════════════════════
# Get keys from: https://dashboard.moyasar.com/settings
# Webhook URL: https://your-domain.com/api/payments/webhook

# Publishable Key (safe for frontend)
MOYASAR_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXXX

# Secret Key (backend only — NEVER expose!)
MOYASAR_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXXX

# Webhook Secret (optional — for signature verification)
MOYASAR_WEBHOOK_SECRET=
